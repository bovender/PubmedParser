name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ "*" ]

jobs:
  test:
    name: "PHPUnit: MW ${{ matrix.mw }}, PHP ${{ matrix.php }}"
    strategy:
      fail-fast: false
      matrix:
        include:
          # Current Long-Term Support release
          - mw: 'REL1_43'
            php: '8.1'
          # Legacy stable release
          - mw: 'REL1_45'
            php: '8.2'
          # Current stable release
          - mw: 'REL1_46'
            php: '8.2'
          # Development branch, to catch breakage early
          - mw: 'master'
            php: '8.3'
    runs-on: ubuntu-latest
    continue-on-error: ${{ matrix.mw == 'master' }}
    steps:
      # check out the repository
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test extension
        # Pinned to a commit rather than the v2 tag: v2 uses actions/cache@v2,
        # which GitHub has disabled; this commit (post-v2, pre-v3) fixes that.
        uses: wikiteq/mediawiki-phpunit-action@149d6c3cbd8c80b2685100f2aaa26f8878a4f0fc
        with:
          type: extension
          php: ${{ matrix.php }}
          mwbranch: ${{ matrix.mw }}
          extension: PubmedParser
